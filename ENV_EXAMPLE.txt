# Curie Application Environment Variables
# Copy this file to .env and fill in your actual values

# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here

# Application URLs
PUBLIC_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000

# Database Configuration (Supabase PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# Security Configuration
# IMPORTANT: Generate a strong random secret key for production!
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production

# Admin User Configuration (optional - for auto-creation on startup)
# Note: No default admin accounts. Create users via signup flow.
# Removed: No auto-admin creation for security

# Logging
LOG_LEVEL=INFO

# OpenAI API Key (optional - leave empty for rule-based AI)
OPENAI_API_KEY=

# Maintenance Mode (set to 'true' to pause the website)
# Backend: Blocks all API requests except /health
# Frontend: Shows maintenance page to all visitors
MAINTENANCE_MODE=false
NEXT_PUBLIC_MAINTENANCE_MODE=false

